### Test with Electron + Flask

- Run `npm i` in `genetic-frontend` 
- Make sure flask's `requirements.txt` are pip installed
- Run everything with `npm run start` in the `genetic-frontend` folder.
### Genetic Circuits Tool
